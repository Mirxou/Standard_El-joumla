# 🚀 البدء السريع - اختبارات Playwright
# Quick Start - Playwright Testing

## ⚡ التثبيت السريع

### 1. تثبيت المتطلبات (خطوة واحدة):

```bash
pip install -r requirements-test.txt
playwright install
```

---

## 🧪 تشغيل الاختبارات

### الطريقة الأولى: استخدام الملفات الدفعية (الأسهل)

#### اختبار كامل للنظام:
```bash
run_tests.bat
```

#### اختبارات Playwright فقط:
```bash
run_playwright_tests.bat
```

### الطريقة الثانية: أوامر مباشرة

#### اختبارات سريعة (Smoke):
```bash
pytest tests/ -m smoke -v
```

#### جميع الاختبارات:
```bash
pytest tests/ -v --html=test_reports/report.html
```

#### مع التغطية:
```bash
pytest tests/ --cov=src --cov-report=html
```

---

## 📊 عرض التقارير

بعد التشغيل، افتح التقرير:

```bash
start test_reports\all_tests.html
```

أو تقرير التغطية:

```bash
start test_reports\coverage\index.html
```

---

## 🎯 الأوامر الأكثر استخداماً

| الأمر | الوصف |
|-------|-------|
| `pytest tests/` | تشغيل جميع الاختبارات |
| `pytest tests/ -v` | مع تفاصيل أكثر |
| `pytest tests/ -m smoke` | اختبارات Smoke فقط |
| `pytest tests/ -m ui` | اختبارات الواجهة فقط |
| `pytest tests/ -k test_name` | اختبار محدد |
| `pytest tests/ --lf` | إعادة الفاشلة فقط |
| `pytest tests/ -x` | توقف عند أول فشل |

---

## 📁 الهيكل

```
tests/
├── conftest.py              # إعدادات pytest
├── e2e/
│   ├── test_main_window.py  # اختبارات النافذة الرئيسية
│   └── test_workflows.py    # اختبارات سير العمل
└── unit/                    # (للإضافة لاحقاً)
```

---

## ✅ Checklist سريع

- [ ] تثبيت المتطلبات: `pip install -r requirements-test.txt`
- [ ] تثبيت Playwright: `playwright install`
- [ ] تشغيل الاختبارات: `run_tests.bat`
- [ ] فحص التقارير في `test_reports/`
- [ ] التأكد من نسبة النجاح 100%

---

## 🆘 مشاكل شائعة

### المشكلة: "playwright command not found"
**الحل:**
```bash
pip install playwright
playwright install
```

### المشكلة: الاختبارات تفشل
**الحل:**
- تأكد من تشغيل التطبيق أولاً للاختبارات التي تحتاج ذلك
- تحقق من قاعدة البيانات

### المشكلة: encoding errors
**الحل:**
```bash
# في PowerShell
$env:PYTHONIOENCODING="utf-8"
pytest tests/
```

---

## 📚 مزيد من التفاصيل

راجع **TESTING_GUIDE.md** للدليل الكامل.

---

**نصيحة:** ابدأ باختبارات Smoke أولاً للتأكد من أن كل شيء يعمل!

```bash
pytest tests/ -m smoke -v
```
