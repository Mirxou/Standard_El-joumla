# ✅ الإصلاحات المطبقة - الرسوم البيانية

## الأخطاء التي تم إصلاحها

### 1. ✅ خطأ ValueError في رسم المخزون
**الخطأ:**
```
ValueError: invalid literal for int() with base 10: '-'
```

**السبب:** الكود كان يحاول تحويل "-" (النص الافتراضي للقيم الفارغة) إلى int.

**الحل:**
- إضافة دالة `safe_int_from_label()` لمعالجة النصوص بشكل آمن
- التحقق من القيم الفارغة و "-" قبل التحويل
- استخدام `float()` ثم `int()` للتحويل الآمن
- إرجاع قيمة افتراضية (0) في حالة الخطأ

### 2. ✅ خطأ UnboundLocalError في تحميل العمليات
**الخطأ:**
```
cannot access local variable 'activities' where it is not associated with a value
```

**السبب:** متغير `activities` كان يُستخدم قبل تعريفه في دالة `load_activities_and_analytics()`.

**الحل:**
- تعريف `activities = []` في بداية الدالة (السطر 1491)
- إرجاع بيانات فارغة بدلاً من `None` في حالة الخطأ

## التعديلات المطبقة

### ملف: `src/ui/windows/main_window.py`

1. **دالة `safe_int_from_label()` (السطر 2229-2244):**
   - معالجة آمنة للنصوص
   - تجاهل "-" والقيم الفارغة
   - تحويل آمن باستخدام float ثم int

2. **دالة `load_activities_and_analytics()` (السطر 1491):**
   - تعريف `activities = []` في البداية
   - إرجاع بيانات فارغة بدلاً من None

## النتيجة

✅ **لا مزيد من الأخطاء:**
- لا `ValueError` عند رسم المخزون
- لا `UnboundLocalError` عند تحميل العمليات
- الرسوم البيانية تعمل بشكل صحيح
- معالجة آمنة للقيم الفارغة

## الخطوة التالية

**أعد تشغيل التطبيق:**
```bash
python main.py
```

يجب أن يعمل بدون أخطاء الآن! 🎉

---

**تاريخ الإصلاح:** 2025-11-30  
**الحالة:** ✅ تم الإصلاح

