# الدليل العملي لاستخدام ستاندرد الجملة
# Standard El-Joumla - Practical Usage Guide

---

## 📋 المتطلبات الأساسية

| المتطلب | الإصدار | ملاحظة |
|---------|---------|--------|
| Python | 3.11+ (يفضل 3.13) | مطلوب |
| pip | أحدث إصدار | مطلوب |
| نظام التشغيل | Windows 10/11 | مدعوم بالكامل |
| مساحة القرص | ~500 ميجابايت | للمكتبات والبيانات |

> **ملاحظة**: Node.js مطلوب فقط إذا كنت ستستخدم واجهة الويب (Next.js).

---

## 🚀 التشغيل للمرة الأولى (خطوة بخطوة)

### الخطوة 1: إنشاء البيئة الافتراضية
```powershell
cd "c:\Users\aboun\Desktop\Standard El-Joumla trae"
python -m venv .venv
.venv\Scripts\activate
```

### الخطوة 2: تثبيت المكتبات
```powershell
pip install -r requirements.txt
```
> ⚠️ إذا فشل تثبيت WeasyPrint، يمكنك تجاهله — التطبيق يعمل بدونه (يُستخدم فقط لتصدير PDF)

### الخطوة 3: تشغيل التطبيق
```powershell
python main.py
```

### الخطوة 4: تسجيل الدخول
عند التشغيل الأول، يتم إنشاء مستخدم مدير تلقائياً:

| الحقل | القيمة |
|-------|--------|
| **اسم المستخدم** | `admin` |
| **كلمة المرور** | `admin123` |

> 🔒 **مهم**: غيّر كلمة المرور فوراً بعد أول تسجيل دخول من إعدادات المستخدم.

---

## 🖥️ واجهة التطبيق الرئيسية

عند تسجيل الدخول بنجاح، تظهر النافذة الرئيسية مع:

### الأقسام الرئيسية
1. **لوحة المعلومات** — ملخص يومي للمبيعات والمخزون
2. **المنتجات** — إدارة المنتجات والفئات والباركود
3. **المبيعات (POS)** — نقطة البيع لإنشاء الفواتير
4. **المشتريات** — تسجيل فواتير الشراء من الموردين
5. **العملاء** — إدارة بيانات العملاء وأرصدتهم
6. **الموردين** — إدارة الموردين والتواصل
7. **التقارير** — تقارير شاملة مع تصدير PDF/Excel
8. **الإعدادات** — إعدادات النظام والمستخدمين

---

## 📦 سيناريوهات الاستخدام الأساسية

### 1️⃣ إضافة منتج جديد
1. افتح قسم **المنتجات**
2. اضغط **إضافة منتج جديد**
3. املأ البيانات:
   - **الاسم**: اسم المنتج بالعربية
   - **الباركود**: رقم الباركود (اختياري)
   - **الفئة**: اختر من القائمة (إلكترونيات، ملابس، إلخ)
   - **سعر الشراء**: تكلفة المنتج
   - **سعر البيع**: سعر البيع للمستهلك
   - **سعر الجملة**: سعر البيع بالجملة (اختياري)
   - **الحد الأدنى للمخزون**: الكمية التي يتم التنبيه عندها
   - **الكمية الحالية**: الرصيد الأولي
4. اضغط **حفظ**

### 2️⃣ تسجيل فاتورة بيع
1. افتح قسم **المبيعات**
2. اختر العميل (أو اتركه فارغاً للبيع النقدي)
3. أضف المنتجات:
   - بالباركود: امسح الباركود بالقارئ
   - بالاسم: ابحث واختر من القائمة
4. حدد **الكمية** لكل منتج
5. اختر **طريقة الدفع**: نقدي / بطاقة / آجل
6. اضغط **إتمام البيع**

> التطبيق يقوم تلقائياً بـ:
> - خصم الكمية من المخزون
> - تحديث رصيد العميل (إذا كان آجل)
> - تسجيل حركة المخزون
> - إنشاء قيد محاسبي

### 3️⃣ إلغاء فاتورة
1. من **المبيعات** → **الفواتير السابقة**
2. اختر الفاتورة المطلوبة
3. اضغط **إلغاء الفاتورة**
4. أدخل **سبب الإلغاء**
5. التأكيد

> التطبيق يقوم تلقائياً بـ:
> - إرجاع الكميات للمخزون
> - تعديل رصيد العميل
> - تسجيل الحركة في سجل التدقيق

### 4️⃣ تسجيل فاتورة شراء
1. افتح قسم **المشتريات**
2. اختر **المورد**
3. أضف المنتجات مع الكميات والأسعار
4. اضغط **حفظ**

> يتم تلقائياً:
> - زيادة مخزون المنتجات
> - تسجيل حركة المخزون
> - تحديث رصيد المورد

### 5️⃣ استخراج تقرير
1. افتح قسم **التقارير**
2. اختر نوع التقرير:
   - **تقرير المبيعات**: حسب الفترة
   - **تقرير المخزون**: الأرصدة الحالية
   - **تقرير الأرباح**: الإيرادات مقابل التكاليف
   - **تقرير العملاء**: الأرصدة والديون
3. حدد الفترة الزمنية
4. اضغط **عرض** أو **تصدير PDF/Excel**

---

## ⚙️ الإعدادات المهمة

### إعداد معلومات الشركة
من **الإعدادات** → **معلومات الشركة**:
- اسم الشركة (عربي/إنجليزي)
- العنوان
- الهاتف والبريد الإلكتروني
- الرقم الضريبي
- شعار الشركة (يظهر في الفواتير)

### إعداد الطباعة
من **الإعدادات** → **الطباعة**:
- اختيار الطابعة الافتراضية
- حجم الورق (A4 / إيصال حراري)
- هوامش الطباعة
- طباعة QR Code / Barcode تلقائياً

### إدارة المستخدمين
من **الإعدادات** → **المستخدمين**:
- إنشاء مستخدمين جدد
- تعيين الأدوار: مدير / محاسب / بائع / مستخدم عادي
- تعيين الصلاحيات لكل مستخدم

---

## 🗄️ قاعدة البيانات

### الموقع الافتراضي
```
data/logical_release.db
```

### النسخ الاحتياطي
- **تلقائي**: كل 24 ساعة (قابل للتعديل)
- **يدوي**: من الإعدادات → النسخ الاحتياطي
- **الموقع**: `data/backups/`

### الاستعادة
1. أوقف التطبيق
2. انسخ ملف النسخة الاحتياطية إلى `data/logical_release.db`
3. أعد تشغيل التطبيق

---

## 🌐 تشغيل واجهة الويب (اختياري)

### تشغيل الـ API
```powershell
# من الجذر
python scripts/start-backend.py
# أو مباشرة
uvicorn src.api.app:app --host 0.0.0.0 --port 8000
```

### تشغيل واجهة الويب
```powershell
cd web
npm install
npm run dev
# الوصول: http://localhost:3000
```

---

## ⚠️ ملاحظات مهمة

1. **لا تحذف** ملف `erp_system.db` أو `data/logical_release.db` — يحتوي على جميع بياناتك
2. **خذ نسخة احتياطية** قبل أي تحديث للبرنامج
3. **لا تشارك** ملف `.env` أبداً — يحتوي على مفاتيح الأمان
4. **غيّر كلمة المرور** الافتراضية فوراً (`admin123`)
5. **السمة الداكنة** متاحة من الإعدادات → المظهر

---

## 🔧 استكشاف الأخطاء

| المشكلة | الحل |
|---------|------|
| التطبيق لا يبدأ | تأكد من تفعيل البيئة الافتراضية `.venv\Scripts\activate` |
| خطأ في الاستيراد | شغّل `pip install -r requirements.txt` |
| قاعدة البيانات مقفلة | أغلق جميع نسخ التطبيق وأعد التشغيل |
| شاشة بيضاء | تحقق من سجلات الأخطاء في `logs/` |
| فشل تسجيل الدخول | استخدم `admin` / `admin123` — أو احذف قاعدة البيانات لإعادة الإنشاء |
| WeasyPrint لا يعمل | ثبّت GTK3 من [هنا](https://github.com/niccokunzmann/install_weasyprint_windows) |
