# Integration Test Guide - دليل اختبار التكامل 🧪

## 🎯 الهدف
التحقق من أن WindowManager يعمل بشكل صحيح مع النوافذ المحدثة.

---

## ✅ قائمة الاختبارات

### الاختبار 1: فتح وإغلاق أساسي (2 دقيقة)

**الخطوات:**
1. شغّل التطبيق
2. افتح `PhysicalCountsWindow` من القائمة
3. حرّك النافذة لمكان جديد (مثلاً: يمين الشاشة)
4. غيّر حجم النافذة (مثلاً: اجعلها أكبر)
5. أغلق النافذة (زر X)
6. افتحها مرة أخرى

**النتيجة المتوقعة:**
- ✅ النافذة تفتح في نفس المكان الذي أغلقتها فيه
- ✅ النافذة تحافظ على نفس الحجم
- ✅ لا أخطاء في Console

---

### الاختبار 2: حفظ Toolbar State (3 دقائق)

**الخطوات:**
1. افتح `PhysicalCountsWindow`
2. إذا كان Toolbar قابلاً للتحريك:
   - اسحب Toolbar لمكان آخر (مثلاً: أسفل النافذة)
3. أغلق النافذة
4. افتحها مرة أخرى

**النتيجة المتوقعة:**
- ✅ Toolbar يعود لنفس المكان الذي وضعته فيه
- ✅ ترتيب الأزرار محفوظ

---

### الاختبار 3: Phantom Window Fix (3 دقائق)

**الخطوات:**
1. افتح `PhysicalCountsWindow`
2. صغّر النافذة (Minimize) - زر `_` في الزاوية
3. أغلق التطبيق كاملاً (File → Exit أو Alt+F4)
4. شغّل التطبيق مرة أخرى
5. افتح `PhysicalCountsWindow`

**النتيجة المتوقعة:**
- ✅ النافذة تفتح بحجم طبيعي (ليست صغيرة جداً)
- ✅ النافذة مرئية على الشاشة (ليست خارج الشاشة)
- ✅ النافذة ليست minimized

---

### الاختبار 4: Multi-Monitor (إذا كان متاحاً) (2 دقيقة)

**الخطوات:**
1. افتح `PhysicalCountsWindow`
2. اسحب النافذة لشاشة ثانوية
3. أغلق النافذة
4. افصل الشاشة الثانوية (أو أغلق التطبيق)
5. شغّل التطبيق مرة أخرى
6. افتح النافذة

**النتيجة المتوقعة:**
- ✅ النافذة تفتح على الشاشة الرئيسية (إذا كانت الثانوية غير متاحة)
- ✅ النافذة مرئية وليست خارج الشاشة

---

### الاختبار 5: Singleton Behavior (2 دقيقة)

**الخطوات:**
1. افتح `PhysicalCountsWindow`
2. حاول فتحها مرة أخرى (من نفس الزر)

**النتيجة المتوقعة:**
- ✅ لا يتم إنشاء نافذة جديدة
- ✅ النافذة الموجودة تظهر للأمام (raise)

---

### الاختبار 6: Memory Leak Test (5 دقائق)

**الخطوات:**
1. افتح `PhysicalCountsWindow`
2. أغلقها
3. افتحها مرة أخرى
4. كرر الخطوات 1-3 عشر مرات
5. راقب استخدام الذاكرة (Task Manager)

**النتيجة المتوقعة:**
- ✅ استخدام الذاكرة لا يزداد بشكل مستمر
- ✅ لا Memory Leaks

---

### الاختبار 7: ReportsWindow مع Workers (3 دقائق)

**الخطوات:**
1. افتح `ReportsWindow`
2. توليد تقرير (قد يستغرق وقتاً)
3. أثناء التوليد، أغلق النافذة

**النتيجة المتوقعة:**
- ✅ Worker يتوقف بشكل صحيح
- ✅ لا أخطاء في Console
- ✅ لا Memory Leaks

---

## 📊 تقرير الاختبار

بعد إكمال جميع الاختبارات، املأ هذا التقرير:

```
✅ الاختبار 1: فتح وإغلاق أساسي - [نجح/فشل]
✅ الاختبار 2: حفظ Toolbar State - [نجح/فشل]
✅ الاختبار 3: Phantom Window Fix - [نجح/فشل]
✅ الاختبار 4: Multi-Monitor - [نجح/فشل/غير متاح]
✅ الاختبار 5: Singleton Behavior - [نجح/فشل]
✅ الاختبار 6: Memory Leak Test - [نجح/فشل]
✅ الاختبار 7: ReportsWindow مع Workers - [نجح/فشل]

ملاحظات:
- ...
- ...
```

---

## 🐛 إذا فشل أي اختبار

### المشكلة: النافذة لا تحفظ حالتها
**الحل المحتمل:**
- تحقق من أن `restore_geometry=True` في التسجيل
- تحقق من أن `auto_save_state=True`

### المشكلة: Toolbar لا يحفظ حالته
**الحل المحتمل:**
- تحقق من أن `setObjectName()` موجود
- تحقق من أن `restore_state=True` في التسجيل

### المشكلة: Phantom Window (نافذة صغيرة جداً)
**الحل المحتمل:**
- تحقق من أن `_is_on_screen()` يعمل
- تحقق من أن `_center_window()` يُستدعى عند الفشل

---

## ✅ معايير النجاح

**النظام يعمل بشكل صحيح إذا:**
- ✅ جميع الاختبارات الأساسية (1-5) نجحت
- ✅ لا Memory Leaks واضحة
- ✅ لا أخطاء في Console

**النظام جاهز للإنتاج إذا:**
- ✅ جميع الاختبارات (1-7) نجحت
- ✅ الأداء جيد
- ✅ تجربة المستخدم ممتازة

---

## 🚀 بعد الاختبار الناجح

إذا نجحت جميع الاختبارات:
1. ✅ يمكنك المتابعة بتطبيق النموذج على باقي النوافذ
2. ✅ يمكنك الانتقال لمنطق العمل
3. ✅ النظام جاهز للإنتاج

**حظاً موفقاً في الاختبار!** 🎯

