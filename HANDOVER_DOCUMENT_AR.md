# 📄 وثيقة التسليم والمراجعة الفنية الشاملة لمشروع: تطبيق ستاندرد الجملة (Standard El-Joumla ERP)

> [!NOTE]
> تم إعداد هذه الوثيقة لمساعدة المبرمج البشري المحترف (والمراجع الأكاديمي/الخبير البرمجي) على فهم بنية المشروع، مراجعة الأكواد، رصد الأخطاء الشائعة الناتجة عن الذكاء الاصطناعي، وضمان أمان ونزاهة النظام قبل النشر الفعلي.

---

## 🎯 القسم الأول: مقدمة للمبرمج المراجع والبيئة العامة
أهلاً بك زميلنا المراجع المحترف.
هذا المشروع عبارة عن نظام متكامل لإدارة الأعمال (ERP) مخصص لتجارة الجملة والتجزئة (المواد الغذائية، مواد التجميل، النظافة، والسوبر ماركت). تم بناء النظام أساساً باستخدام مساعد برمجة ذكاء اصطناعي (AI IDE) تحت إشراف وتوجيه مباشر من صاحب العمل لتحديد المتطلبات التجارية المعقدة.

النظام يعمل حالياً بكفاءة ويمتلك تغطية اختبارات واسعة جداً (تفوق 190 ملف اختبار)، ولكنه يتطلب **مراجعة بشرية صارمة (Code Audit)** لضمان الاستقرار، الأمان، وتصحيح بعض الآثار الجانبية المعتادة لأكواد الذكاء الاصطناعي (مثل خلط الملفات، التعليقات بلغات مختلفة، أو البصمات المرجعية للمشاريع الأخرى).

### المطلوب منك في هذه المراجعة:
1. **التدقيق الهيكلي:** التأكد من تماسك العلاقات المحاسبية والمخزنية ومطابقتها للمعايير المعترف بها.
2. **فحص الثغرات الأمنية والمالية:** التركيز على المعاملات الذرية (Atomic Transactions) لمنع حدوث تضارب في الجرد عند انقطاع الاتصال أو الخطأ البرمجي.
3. **تنظيف البصمات (Cleanup):** إزالة أي تعليقات أو بقايا أكواد غير مستخدمة ومكتوبة بلغات أخرى.
4. **تحسين الأداء:** فحص الفهارس (Indexes) وعمليات الاستعلام في قاعدة البيانات لتجنب جلب البيانات بالكامل إلى الذاكرة.

---

## 🏢 القسم الثاني: السياق التجاري وطبيعة التطبيق المعقدة
النظام يعالج عمليات متداخلة تجمع بين بيئة بيع الجملة (مستودعات، كراتين، مناديب) وبيئة التجزئة (بيع بالقطعة/الشريط، أسعار متدرجة، تواريخ صلاحية).

### المتطلبات التجارية الأساسية المبرمجة:
1. **تحويل الوحدات (Unit Conversion):**
   * يدعم النظام الشراء بالكرتون والبيع بالقطعة أو الشريط.
   * عند بيع قطعة وكان رصيد القطع صفراً، يجب أن يخصم النظام كرتوناً واحداً تلقائياً ويحوله إلى قطع مكافئة بناءً على عامل التحويل (Conversion Factor) المسجل للمنتج.
   * **نقطة الفحص:** تأكد من أن عملية التحويل تتم داخل معاملة واحدة (Database Transaction) لتجنب نقص الكرتون دون زيادة القطع في حالة حدوث استثناء.

2. **تواريخ الصلاحية وتتبع الدفعات (Expiry Dates & FEFO):**
   * يطبق النظام مبدأ **المنتج الأقرب لانتهاء الصلاحية يخرج أولاً (FEFO - First Expired, First Out)**.
   * يتم تتبع المنتجات عبر أرقام الدفعات (Batch Numbers).
   * **نقطة الفحص:** تحقق من خوارزمية الصرف التلقائي في خدمات المبيعات ومطابقتها لدفعات المخزون المتوفرة.

3. **التسعير المتدرج والعملاء المميزين:**
   * يدعم النظام تسعير التجزئة، الجملة، ونصف الجملة، بالإضافة إلى التسعير المخصص لشرائح معينة من العملاء (مثل الفنادق أو المطاعم).

4. **الديون والآجل والمحاسبة المتكاملة:**
   * يضم النظام موديول محاسبي يسجل القيود المزدوجة (Double-Entry Bookkeeping) تلقائياً عند كل عملية بيع، شراء، أو سداد.
   * تتبع ذمم العملاء والموردين بدقة مع دعم جدولة المدفوعات.

---

## 🏗️ القسم الثالث: البنية التقنية للمشروع (Tech Stack)

تم اختيار التقنيات بعناية لضمان أداء عالٍ محلياً مع إمكانية المزامنة السحابية:
* **واجهة تطبيق سطح المكتب:** **PySide6 (Qt for Python)** لتقديم واجهة مستخدم حديثة وغامرة (Quantum Dark Theme) تدعم اللغة العربية بالكامل (RTL).
* **الواجهة الخلفية للويب والـ API:** **FastAPI + Uvicorn** لخدمة الطلبات الخارجية والمزامنة.
* **قواعد البيانات:**
  * **SQLite (محلياً):** يتم إدارتها عبر `LocalDatabaseManager` لضمان سرعة العمل بدون إنترنت.
  * **PostgreSQL (سحابياً/اختياري):** مدعومة عبر موديول المزامنة وسيرفر FastAPI للربط المركزي بين الفروع والمستودعات.
* **الذكاء الاصطناعي والتنبؤات:** استخدام `scikit-learn` و `pandas` و `numpy` للتنبؤ بالطلب المستقبلي وتحليل حركة المخزون.
* **محرك التقارير والطباعة:** استخدام `WeasyPrint` لتحويل تقارير HTML الديناميكية إلى ملفات PDF تدعم الخطوط العربية بشكل كامل وصحيح، وتكامل مع طابعات الفواتير الحرارية `ESC/POS`.
* **الحماية والأمان:** تشفير كلمات المرور باستخدام `argon2-cffi` و `bcrypt` وتأمين جلسات العمل عبر `PyJWT` وتشفير البيانات الحساسة باستخدام `cryptography` (AES-256-GCM).

---

## 🗂️ القسم الرابع: الهيكل المجلد الفعلي ومواقع المراجعة

المشروع يتبع نمط التصميم ثلاثي الطبقات (Three-Tier Architecture) مع فصل واضح للمسؤوليات:

```text
Logical Version trae/
├── main.py                     # نقطة الدخول الرئيسية للتطبيق (تهيئة قاعدة البيانات وشاشة البداية)
├── requirements.txt            # قائمة الاعتماديات وحزم بايثون المثبتة
├── pytest.ini                  # إعدادات تشغيل حزمة الاختبارات
├── src/                        # مجلد كود المصدر الرئيسي
│   ├── core/                   # النواة الأساسية (إدارة البيانات، الحماية، التشفير، النسخ الاحتياطي)
│   │   ├── database_manager.py # محرك قاعدة البيانات الأساسي (SQLite / PostgreSQL)
│   │   ├── local_database_manager.py # المحرك المحلي وتدفق البيانات الفعلي للديسكتوب
│   │   ├── audit_trail_manager.py    # تتبع ومراقبة تصرفات المستخدمين والعمليات المالية
│   │   ├── backup_manager.py         # إدارة النسخ الاحتياطية المشفرة والمضغوطة
│   │   └── security_service.py       # إدارة صلاحيات المستخدمين والتحقق الثنائي (2FA/TOTP)
│   ├── services/               # طبقة منطق الأعمال (Business Logic Layer)
│   │   ├── sales_service.py    # معالجة فواتير المبيعات، حساب الخصومات، والنزاهة المالية
│   │   ├── inventory_service.py # الجرد، التنبيهات، تحويل كرتون/قطعة، والربط بالدفعات
│   │   ├── accounting_service.py # القيود المحاسبية، دفتر الأستاذ، وميزان المراجعة
│   │   └── pdf_export_service.py # تصدير الفواتير والتقارير باستخدام WeasyPrint
│   ├── models/                 # نماذج وقواميس البيانات البرمجية
│   ├── ui/                     # واجهات المستخدم المكتبية (نوافذ، حوارات، عناصر مخصصة)
│   ├── api/                    # مسارات الـ FastAPI للربط الشبكي
│   └── utils/                  # وظائف مساعدة (المسجلات Logger، الموثقات Validator)
├── web/                        # تطبيق الويب المعتمد على React/Next.js للمشاهدة الخارجية
└── tests/                      # حزمة الاختبارات الشاملة (أكثر من 190 ملف اختبار)
    └── unit/                   # الاختبارات الوحدوية للخدمات والمكونات الأساسية
```

---

## 🚨 القسم الخامس: نقاط هامة ومكشوفة أثناء المراجعة الفنية (تنبيه للمبرمج)

أثناء المراجعة التقنية والتهيئة للمشروع، تم رصد عدة نقاط هامة يجب على المبرمج المراجع معالجتها أو تفهمها:

1. **تداخل وبصمات مشاريع أخرى (Project Boilerplate Leak):**
   * يحتوي الملف `tests/run_tests.py` في السطر 17 على تسمية مرجعية لمشروع آخر: `"ReadyRent Sovereign Ecosystem - Test Suite Orchestrator"`. هذا خطأ ذكاء اصطناعي شائع ناتج عن استخدام قوالب سابقة. يجب تعديل الاسم ليصبح `"Standard El-Joumla ERP - Test Suite Orchestrator"`.
   
2. **تضارب في توصيف التقنيات:**
   * يدعي الملف `PROJECT_REPORT.md` في السطر 10 و 281 أن المشروع مبني على إطار عمل `Django`. هذا الادعاء غير صحيح! المشروع مبني بالكامل على **FastAPI** في الواجهة الخلفية و **PySide6** لسطح المكتب. تم تصحيح هذا التضارب في تقرير المشروع ليطابق الواقع الفعلي.
   
3. **وجود تعليقات بلغات أخرى (روسية):**
   * يحتوي ذيل ملف `PROJECT_REPORT.md` على خلاصة مكتوبة باللغة الروسية (مثل: `Отчет создан`, `Статус проекта`). تم تنظيفها وتعريبها بالكامل. يرجى التأكد من عدم بقاء أي تعليقات مماثلة في ملفات الخدمات الأخرى.

4. **النزاهة المالية في `SalesService` و `InventoryService`:**
   * يجب التأكد من أن خصم الكميات من الدفعات (Batches) يتم بترتيب الصلاحية (FEFO).
   * تأكد من استخدام ميكانيكية قفل قاعدة البيانات (Database Lock) أو المعاملات الآمنة (Transactions) عند تعديل المخزون لمنع ظروف التسابق (Race Conditions) عند البيع المتزامن من واجهة سطح المكتب وتطبيق الويب.

---

## 🧪 القسم السادس: دليل تشغيل المشروع محلياً وتشغيل الاختبارات

لتهيئة بيئة العمل ومراجعة النظام على جهازك المحلي، اتبع الخطوات التالية:

### 1. إعداد البيئة الافتراضية وتثبيت الاعتماديات:
المشروع يستخدم بيئة افتراضية جاهزة تقع في `.venv`. لتنشيطها وتثبيت كافة المكونات (تم تثبيتها بالفعل في بيئة العمل الحالية):
```powershell
# على نظام Windows (PowerShell)
.venv\Scripts\Activate.ps1

# تثبيت الحزم المطلوبة
pip install -r requirements.txt
```

### 2. تشغيل حزمة الاختبارات الشاملة:
تم تثبيت إضافات `pytest-asyncio` و `pytest-timeout` لضمان تشغيل حزمة الاختبارات الكبيرة بسلاسة وبدون تعليق.
لتشغيل الفحص التلقائي لجميع المكونات:
```powershell
python -m pytest tests/unit/ --tb=short -q
```
*ملاحظة للمراجع:* إذا كنت تعمل في بيئة اختبار بدون واجهة رسومية (Headless CI)، يمكنك تشغيل الاختبارات هكذا:
```powershell
$env:QT_QPA_PLATFORM="offscreen"
python -m pytest tests/unit/
```

### 3. تشغيل التطبيق واختباره:
لتشغيل التطبيق الرئيسي مع واجهة المستخدم:
```powershell
python main.py
```
*بيانات الدخول الافتراضية للنظام (تنشأ تلقائياً في أول تشغيل):*
* **اسم المستخدم:** `admin`
* **كلمة المرور الافتراضية:** `admin123`

---

## 🤝 القسم السابع: التوقعات والتسليم النهائي للمراجع

بعد انتهاء المراجعة الكاملة للأكواد والملفات، يتوقع من المراجع تقديم:
1. **تقرير مراجعة الكود (Code Audit Report):** يركز على سلامة الدوال المالية والرياضية وصحة استعلامات قاعدة البيانات وتجنب ثغرات حقن الاستعلامات (SQL Injection).
2. **التعديلات المقترحة (Refactoring Patches):** رفع الكود بعد تنقيته من التحذيرات أو console.log أو تعليقات الذكاء الاصطناعي غير الهادفة.
3. **التأكيد على تغطية الاختبارات:** الحفاظ على بقاء كامل حزمة الاختبارات (>190 ملف) خضراء وتعمل بدون أخطاء.

---
*تم إعداد وتجهيز هذه الوثيقة لمساعدتكم في إنهاء المراجعة بأعلى معايير الجودة البرمجية.*
