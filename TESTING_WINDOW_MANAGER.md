# Testing Window Manager - اختبار مدير النوافذ

## ✅ تم إنجازه

### الخيار 2: إضافة window_key
- ✅ تم إضافة `window_key` و `window_singleton` و `window_title` لـ **19 نافذة**

### الخيار 1: تحديث main_window.py
- ✅ تم تحديث جميع التسجيلات لاستخدام Class-based registration

---

## 🧪 خطوات الاختبار التدريجي (الخيار 3)

### 📋 الخطوة 1: اختبار أساسي (5 دقائق)

#### اختبار ReportsWindow:
1. شغّل التطبيق: `python main.py`
2. افتح `ReportsWindow` من القائمة → **"📊 التقارير" → "📊 التقارير المتقدمة"**
3. تحقق من:
   - ✅ النافذة تفتح بدون أخطاء
   - ✅ النافذة مرئية في الشاشة
   - ✅ لا توجد أخطاء في Console

#### اختبار Singleton Behavior:
1. افتح `ReportsWindow` مرة أخرى (من نفس القائمة)
2. تحقق من:
   - ✅ لا يتم إنشاء نافذة جديدة
   - ✅ النافذة الحالية تُرفع إلى المقدمة

#### اختبار حفظ/استعادة الحالة:
1. حرّك النافذة لمكان جديد (مثلاً: يمين الشاشة)
2. غيّر حجم النافذة (اجعلها أكبر قليلاً)
3. أغلق النافذة (زر X)
4. افتح النافذة مرة أخرى
5. تحقق من:
   - ✅ النافذة عادت لنفس المكان
   - ✅ النافذة حافظت على نفس الحجم

---

### 📋 الخطوة 2: اختبار نوافذ متعددة (10 دقائق)

#### اختبار 3-4 نوافذ مختلفة:
1. افتح `ReportsWindow`
2. افتح `DashboardWindow`
3. افتح `AccountsWindow`
4. افتح `PhysicalCountsWindow`

تحقق من:
- ✅ جميع النوافذ تفتح بشكل صحيح
- ✅ كل نافذة مستقلة عن الأخرى
- ✅ يمكن فتح/إغلاق كل نافذة بشكل منفصل

#### اختبار حفظ حالة متعددة:
1. حرّك كل نافذة لمكان مختلف
2. غيّر حجم كل نافذة بشكل مختلف
3. أغلق جميع النوافذ
4. افتحها مرة أخرى (بالترتيب)
5. تحقق من:
   - ✅ كل نافذة عادت لنفس المكان والحجم

---

### 📋 الخطوة 3: اختبار متقدم (5 دقائق)

#### اختبار Maximized State:
1. افتح نافذة
2. كبّر النافذة (Maximize)
3. أغلق النافذة
4. افتحها مرة أخرى
5. تحقق من:
   - ✅ النافذة تفتح بحالة Maximized

#### اختبار منع Memory Leaks:
1. افتح 5-10 نوافذ
2. أغلقها جميعاً
3. افتح Task Manager
4. تحقق من:
   - ✅ استخدام الذاكرة لم يرتفع بشكل كبير
   - ✅ لا توجد نوافذ "مثلثة" في الذاكرة

---

## 🔍 قائمة التحقق النهائية

### ✅ إذا نجحت جميع الخطوات:
- [ ] جميع النوافذ تفتح بشكل صحيح
- [ ] Singleton behavior يعمل
- [ ] حفظ/استعادة الحالة يعمل
- [ ] لا توجد memory leaks
- [ ] لا توجد أخطاء في Console

### ⚠️ إذا فشلت أي خطوة:
1. راجع السجلات: `logs/__main__.log`
2. تحقق من Console لأي أخطاء
3. تأكد من أن `window_key` موجود في النافذة
4. تأكد من أن التسجيل في `main_window.py` صحيح

---

## 📊 النتيجة المتوقعة

بعد إكمال جميع الخطوات:
- ✅ **النظام يعمل بشكل صحيح!**
- ✅ جاهز للإنتاج
- ✅ يمكنك المتابعة لتطبيق الميزات الأخرى

---

## 🚀 بعد الاختبار الناجح

يمكنك الآن:
1. ✅ استخدام `auto_register` لتسجيل النوافذ تلقائياً (اختياري)
2. ✅ إضافة hooks للنوافذ (اختياري)
3. ✅ الانتقال لمنطق العمل
4. ✅ النظام جاهز للإنتاج

**حظاً موفقاً!** 🎯

