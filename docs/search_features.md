# نظام البحث المتقدم - Advanced Search System

## نظرة عامة

نظام البحث المتقدم هو أداة قوية تمكن المستخدمين من البحث السريع والفعال في جميع بيانات النظام باستخدام مجموعة من المعايير والفلاتر المتقدمة.

## الميزات الرئيسية

### 1. البحث متعدد الكيانات (Multi-Entity Search)

يدعم النظام البحث في 8 أنواع من الكيانات:
- **المنتجات** (Products): البحث في الأسماء، الأكواد، الأوصاف
- **العملاء** (Customers): البحث في الأسماء، الهواتف، العناوين
- **الموردون** (Suppliers): البحث في الأسماء، معلومات الاتصال
- **المبيعات** (Sales): البحث في أرقام الفواتير، بيانات العملاء
- **المشتريات** (Purchases): البحث في أرقام الفواتير، بيانات الموردين
- **عروض الأسعار** (Quotes): البحث في أرقام العروض، حالة العرض
- **المرتجعات** (Returns): البحث في أرقام المرتجعات، أسباب الإرجاع
- **الحسابات المحاسبية** (Accounts): البحث في أسماء الحسابات، الأكواد

### 2. البحث بالكلمات المفتاحية

- **بحث سريع**: أدخل كلمة مفتاحية للبحث في جميع الحقول المهمة
- **بحث ذكي**: اقتراحات تلقائية أثناء الكتابة (Auto-suggestions)
- **خيارات البحث**:
  - حساسية الأحرف الكبيرة/الصغيرة (Case Sensitive)
  - البحث عن كلمة كاملة (Whole Word)
  - تضمين السجلات غير النشطة (Include Inactive)

### 3. الفلاتر المتقدمة (Advanced Filters)

النظام يدعم 14 نوعاً من عوامل التصفية:

| العامل | الوصف | مثال |
|--------|-------|------|
| `EQUALS` | يساوي تماماً | السعر = 100 |
| `NOT_EQUALS` | لا يساوي | الحالة ≠ ملغي |
| `CONTAINS` | يحتوي على | الاسم يحتوي "أحمد" |
| `NOT_CONTAINS` | لا يحتوي على | الوصف لا يحتوي "قديم" |
| `STARTS_WITH` | يبدأ بـ | الكود يبدأ بـ "PRD" |
| `ENDS_WITH` | ينتهي بـ | الهاتف ينتهي بـ "123" |
| `GREATER_THAN` | أكبر من | الكمية > 50 |
| `GREATER_EQUAL` | أكبر أو يساوي | السعر >= 100 |
| `LESS_THAN` | أصغر من | المخزون < 10 |
| `LESS_EQUAL` | أصغر أو يساوي | الخصم <= 20 |
| `BETWEEN` | بين قيمتين | التاريخ بين 2024-01-01 و 2024-12-31 |
| `IN` | ضمن قائمة | الحالة في [مكتمل، قيد المعالجة] |
| `IS_NULL` | فارغ/null | ملاحظات فارغة |
| `IS_NOT_NULL` | غير فارغ | تاريخ التسليم محدد |

### 4. الفلاتر المحفوظة (Saved Filters)

- **حفظ الفلاتر**: احفظ معايير البحث المستخدمة بشكل متكرر
- **فلاتر افتراضية**: النظام يتضمن 4 فلاتر جاهزة:
  - منتجات أقل من الحد الأدنى (Low Stock Products)
  - عملاء لديهم رصيد (Customers with Balance)
  - مبيعات هذا الشهر (This Month Sales)
  - عروض معلقة (Pending Quotes)
- **مشاركة الفلاتر**: إمكانية مشاركة الفلاتر مع المستخدمين الآخرين
- **إدارة الفلاتر**: تحميل، حذف، تعديل الفلاتر المحفوظة

### 5. عرض النتائج

- **جدول ديناميكي**: يتكيف الجدول تلقائياً مع نوع الكيان المختار
- **أعمدة مخصصة**: كل نوع كيان له أعمدة خاصة به
- **ترتيب مرن**: إمكانية الترتيب حسب أي عمود
- **تقسيم الصفحات** (Pagination):
  - 50 نتيجة في الصفحة
  - أزرار التنقل (السابق/التالي)
  - عداد الصفحات والنتائج

### 6. تصدير النتائج

- تصدير نتائج البحث إلى ملف CSV
- احتفاظ بجميع الأعمدة والقيم
- سهولة معالجة البيانات في برامج خارجية (Excel, Google Sheets)

## كيفية الاستخدام

### البحث البسيط

1. افتح نافذة البحث المتقدم من القائمة: `أدوات > 🔍 البحث المتقدم`
2. اختر نوع الكيان من القائمة المنسدلة
3. أدخل الكلمة المفتاحية في حقل البحث
4. اضغط على "بحث" أو Enter

### البحث باستخدام الفلاتر

1. اختر نوع الكيان
2. اضغط على "إضافة فلتر" من الشريط
3. حدد الحقل المطلوب
4. اختر عامل التصفية (يساوي، يحتوي، أكبر من، إلخ)
5. أدخل القيمة المطلوبة
6. اضغط على "بحث"

### حفظ الفلتر

1. قم بإعداد معايير البحث المطلوبة
2. اضغط على "حفظ الفلتر"
3. أدخل اسم ووصف للفلتر
4. حدد ما إذا كنت تريد مشاركته مع الآخرين
5. اضغط على "حفظ"

### تحميل فلتر محفوظ

1. انقر على الفلتر المطلوب من قائمة "الفلاتر المحفوظة"
2. سيتم تحميل المعايير تلقائياً
3. اضغط على "بحث" لتنفيذ الفلتر

## أمثلة عملية

### مثال 1: البحث عن منتجات نفدت من المخزون

```
نوع الكيان: المنتجات
فلتر: current_stock <= 0
```

### مثال 2: عملاء لديهم ديون

```
نوع الكيان: العملاء
فلتر: current_balance > 0
```

### مثال 3: مبيعات شهر محدد

```
نوع الكيان: المبيعات
فلتر: sale_date BETWEEN '2024-01-01' AND '2024-01-31'
```

### مثال 4: منتجات تبدأ بكود معين

```
نوع الكيان: المنتجات
كلمة مفتاحية: PRD
خيار: يبدأ بـ
```

## البنية التقنية

### النماذج (Models)

- `SearchEntity`: تعداد أنواع الكيانات القابلة للبحث
- `FilterOperator`: تعداد عوامل التصفية المتاحة
- `SearchFilter`: نموذج الفلتر الفردي
- `SearchQuery`: نموذج استعلام البحث الكامل
- `SavedFilter`: نموذج الفلتر المحفوظ
- `SearchResult`: نموذج نتيجة البحث

### الخدمات (Services)

- `AdvancedSearchService`: الخدمة الرئيسية للبحث
  - `search()`: تنفيذ استعلام البحث
  - `get_suggestions()`: الحصول على اقتراحات تلقائية
  - `save_filter()`: حفظ فلتر جديد
  - `load_filter()`: تحميل فلتر محفوظ
  - `list_saved_filters()`: عرض جميع الفلاتر المحفوظة
  - `delete_filter()`: حذف فلتر محفوظ

### قاعدة البيانات

#### جدول الفلاتر المحفوظة
```sql
CREATE TABLE saved_filters (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    description TEXT,
    entity TEXT NOT NULL,
    query_data TEXT NOT NULL,  -- JSON
    is_default BOOLEAN DEFAULT 0,
    is_shared BOOLEAN DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

#### جدول سجل البحث
```sql
CREATE TABLE search_history (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,
    entity TEXT,
    keyword TEXT,
    query_data TEXT,  -- JSON
    results_count INTEGER,
    execution_time_ms INTEGER,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
)
```

## الاختصارات

- `Ctrl+F`: فتح نافذة البحث المتقدم
- `Enter`: تنفيذ البحث
- `Ctrl+S`: حفظ الفلتر الحالي (عند التركيز على النافذة)
- `Ctrl+E`: تصدير النتائج

## نصائح وأفضل الممارسات

1. **استخدم الفلاتر المحفوظة**: احفظ عمليات البحث المتكررة لتوفير الوقت
2. **استخدم عوامل التصفية المناسبة**: اختر العامل الأنسب لنوع البيانات
3. **دمج الفلاتر**: يمكنك استخدام عدة فلاتر معاً للحصول على نتائج دقيقة
4. **البحث الشامل**: اختر "الكل" للبحث في جميع الكيانات دفعة واحدة
5. **تصدير البيانات**: صدّر النتائج للتحليل الخارجي أو إعداد التقارير

## التكامل مع النظام

نظام البحث المتقدم يتكامل بسلاسة مع:
- نظام إدارة المنتجات
- نظام إدارة العملاء والموردين
- نظام المبيعات والمشتريات
- نظام عروض الأسعار والمرتجعات
- النظام المحاسبي

## الأداء والكفاءة

- **فهرسة قاعدة البيانات**: جميع الحقول القابلة للبحث مفهرسة
- **تخزين مؤقت**: النتائج المتكررة يتم تخزينها مؤقتاً
- **تحميل تدريجي**: استخدام التقسيم الصفحي لتحسين الأداء
- **استعلامات محسّنة**: استعلامات SQL محسّنة لتقليل وقت التنفيذ

## الدعم والمساعدة

للمساعدة أو الإبلاغ عن مشاكل:
- راجع دليل المستخدم الرئيسي
- تواصل مع فريق الدعم الفني
- استشر قسم الأسئلة الشائعة (FAQ)

---

**إصدار التوثيق**: 1.0  
**آخر تحديث**: 2024  
**متوافق مع**: الإصدار المنطقي v1.0+
