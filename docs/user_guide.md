# دليل المستخدم - نظام إدارة المحاسبة

## مقدمة
هذا دليل شامل لاستخدام نظام إدارة المحاسبة المطور بتقنية PySide6. يغطي هذا الدليل جميع الميزات والوظائف المتاحة في التطبيق.

## البدء السريع

### تشغيل التطبيق
1. تأكد من تثبيت Python 3.8 أو أحدث
2. قم بتشغيل الملف `main.py`
3. سيتم إنشاء قاعدة البيانات تلقائياً عند التشغيل الأول

### الواجهة الرئيسية
عند تشغيل التطبيق، ستظهر النافذة الرئيسية التي تحتوي على:
- شريط القوائم العلوي
- شريط الأدوات
- منطقة العمل الرئيسية
- شريط الحالة السفلي

## القوائم الرئيسية

### 1. قائمة الملف
- **جديد**: إنشاء ملف جديد
- **فتح**: فتح ملف موجود
- **حفظ**: حفظ العمل الحالي
- **خروج**: إغلاق التطبيق

### 2. قائمة العرض
- **شريط الأدوات**: إظهار/إخفاء شريط الأدوات
- **شريط الحالة**: إظهار/إخفاء شريط الحالة
- **ملء الشاشة**: التبديل إلى وضع ملء الشاشة

### 3. قائمة المدفوعات والحسابات
- **📊 لوحة تحكم المدفوعات**: الوصول إلى لوحة التحكم التحليلية
- **💰 إدارة المدفوعات**: إدارة المعاملات المالية
- **📈 التقارير المالية**: عرض التقارير والإحصائيات

### 4. قائمة المساعدة
- **حول**: معلومات عن التطبيق
- **دليل المستخدم**: الوصول إلى هذا الدليل

## لوحة تحكم المدفوعات

### الوصول إلى لوحة التحكم
1. من القائمة الرئيسية، اختر "المدفوعات والحسابات"
2. انقر على "📊 لوحة تحكم المدفوعات"
3. ستفتح نافذة جديدة تحتوي على لوحة التحكم

### مكونات لوحة التحكم

#### مؤشرات الأداء الرئيسية (KPIs)
تعرض في الجزء العلوي من لوحة التحكم:
- **إجمالي المدفوعات**: المبلغ الإجمالي للمدفوعات
- **عدد المعاملات**: إجمالي عدد المعاملات
- **متوسط قيمة المعاملة**: متوسط قيمة المعاملة الواحدة
- **معدل النمو**: نسبة النمو مقارنة بالفترة السابقة
- **الرصيد المتاح**: الرصيد النقدي المتاح
- **المدفوعات المعلقة**: قيمة المدفوعات غير المكتملة

#### التبويبات التحليلية

##### تبويب الاتجاهات
- رسم بياني خطي لاتجاهات المدفوعات الشهرية
- رسم دائري لتوزيع أنواع المدفوعات

##### تبويب التوقعات
- توقعات المدفوعات المستقبلية
- تحليل التدفق النقدي

##### تبويب المقارنات
- مقارنة أداء الفترات الزمنية
- تحليل أعمار الذمم

### الفلاتر والتحكم

#### فلتر الفترة الزمنية
- اختيار فترة زمنية محددة
- استخدام فترات محددة مسبقاً (يوم، أسبوع، شهر، سنة)

#### فلتر نوع المدفوعات
- نقدي
- بنكي
- إلكتروني
- جميع الأنواع

#### أزرار التحكم
- **تحديث**: تحديث البيانات يدوياً
- **تصدير**: تصدير البيانات (قريباً)
- **طباعة**: طباعة التقرير (قريباً)

## إدارة البيانات

### قاعدة البيانات
- يستخدم التطبيق قاعدة بيانات SQLite
- يتم إنشاء قاعدة البيانات تلقائياً عند التشغيل الأول
- يتم إنشاء فئات افتراضية للبيانات

### النسخ الاحتياطي
- يُنصح بعمل نسخة احتياطية من ملف قاعدة البيانات بانتظام
- موقع قاعدة البيانات: `data/accounting.db`

## الميزات المتقدمة

### التحديث التلقائي
- تحديث البيانات كل 5 دقائق تلقائياً
- إمكانية تعطيل التحديث التلقائي من الإعدادات

### معالجة الأخطاء
- عرض رسائل خطأ واضحة ومفيدة
- تسجيل الأخطاء في ملفات السجل
- استرداد تلقائي من الأخطاء البسيطة

### الأداء
- معالجة البيانات في خيوط منفصلة
- عدم تجميد واجهة المستخدم أثناء العمليات الطويلة
- تحسين استعلامات قاعدة البيانات

## اختصارات لوحة المفاتيح

### اختصارات عامة
- `Ctrl + N`: جديد
- `Ctrl + O`: فتح
- `Ctrl + S`: حفظ
- `Ctrl + Q`: خروج
- `F11`: ملء الشاشة

### اختصارات لوحة التحكم
- `F5`: تحديث البيانات
- `Ctrl + P`: طباعة (قريباً)
- `Ctrl + E`: تصدير (قريباً)

## استكشاف الأخطاء وإصلاحها

### المشاكل الشائعة

#### التطبيق لا يبدأ
1. تأكد من تثبيت Python بشكل صحيح
2. تحقق من تثبيت جميع المكتبات المطلوبة
3. تأكد من وجود الملفات المطلوبة

#### لا تظهر البيانات في لوحة التحكم
1. تأكد من وجود بيانات في قاعدة البيانات
2. تحقق من اتصال قاعدة البيانات
3. جرب تحديث البيانات يدوياً

#### بطء في الأداء
1. تحقق من حجم قاعدة البيانات
2. أعد تشغيل التطبيق
3. تأكد من توفر ذاكرة كافية

### رسائل الخطأ الشائعة

#### "Database connection failed"
- تحقق من وجود ملف قاعدة البيانات
- تأكد من صلاحيات الكتابة في مجلد البيانات

#### "Chart rendering error"
- تأكد من تثبيت مكتبة QtCharts
- أعد تشغيل التطبيق

## الدعم والمساعدة

### الحصول على المساعدة
- راجع هذا الدليل أولاً
- تحقق من ملفات السجل للأخطاء
- تواصل مع فريق الدعم الفني

### الإبلاغ عن الأخطاء
عند الإبلاغ عن خطأ، يرجى تضمين:
- وصف مفصل للمشكلة
- خطوات إعادة إنتاج المشكلة
- رسائل الخطأ (إن وجدت)
- معلومات النظام

## التحديثات المستقبلية

### الميزات المخططة
- تصدير التقارير إلى PDF و Excel
- إضافة المزيد من أنواع الرسوم البيانية
- تحسين واجهة المستخدم
- إضافة إشعارات للتحديثات المهمة
- دعم قواعد بيانات متعددة
- واجهة ويب (اختيارية)

### كيفية التحديث
- سيتم إشعارك بالتحديثات الجديدة
- اتبع تعليمات التحديث المرفقة
- احتفظ بنسخة احتياطية قبل التحديث

---

**ملاحظة**: هذا الدليل يغطي الإصدار الحالي من التطبيق. قد تتم إضافة ميزات جديدة أو تعديل الموجودة في الإصدارات المستقبلية.

**تاريخ آخر تحديث**: ديسمبر 2024