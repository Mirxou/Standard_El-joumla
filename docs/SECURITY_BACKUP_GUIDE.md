# دليل الأمان والنسخ الاحتياطي

## 🔐 المصادقة الثنائية (2FA)

### لماذا 2FA؟

توفر المصادقة الثنائية طبقة أمان إضافية تحمي حسابك حتى لو تم اختراق كلمة المرور.

### تفعيل 2FA

#### الخطوة 1: فتح إعدادات الأمان

1. افتح التطبيق وسجل دخولك
2. من القائمة الرئيسية: **أدوات → إدارة التشفير** أو **إعدادات الأمان**
3. اضغط على زر **"تفعيل المصادقة الثنائية (2FA)"**

#### الخطوة 2: مسح رمز QR

1. سيظهر رمز QR على الشاشة
2. افتح تطبيق المصادقة على هاتفك:
   - **Google Authenticator** (موصى به)
   - **Microsoft Authenticator**
   - **Authy**
   - **1Password**
   - أي تطبيق متوافق مع TOTP

3. امسح رمز QR باستخدام التطبيق

#### الخطوة 3: حفظ رموز الاسترداد

```
⚠️ هام جداً: احفظ رموز الاسترداد في مكان آمن!
```

**رموز الاسترداد تظهر مرة واحدة فقط**

مثال:
```
ABCD-1234-EFGH-5678
IJKL-9012-MNOP-3456
...
```

**أماكن حفظ آمنة:**
- ✅ برنامج إدارة كلمات المرور (1Password, Bitwarden)
- ✅ ملف مشفر على قرص خارجي
- ✅ ورقة في خزانة آمنة
- ❌ لا تحفظها على نفس الجهاز
- ❌ لا ترسلها عبر البريد الإلكتروني

#### الخطوة 4: التأكيد

1. أدخل الرمز المكون من 6 أرقام من التطبيق
2. اضغط **"تأكيد"**
3. ستظهر رسالة نجاح ✅

### تسجيل الدخول مع 2FA

بعد التفعيل، سيطلب منك:

1. **اسم المستخدم** ←
2. **كلمة المرور** ←
3. **رمز 2FA** (6 أرقام من التطبيق) ←

**ملاحظات:**
- الرمز يتجدد كل 30 ثانية
- لديك نافذة زمنية قصيرة للإدخال
- أدخل الأرقام فقط (بدون مسافات أو شرطات)

### في حالة فقدان الهاتف

**السيناريو 1: لديك رموز الاسترداد**

1. اضغط **"استخدام رمز الاسترداد"** في شاشة تسجيل الدخول
2. أدخل أحد رموز الاسترداد
3. سجل دخولك وأعد تهيئة 2FA فوراً

**السيناريو 2: لا تملك رموز الاسترداد**

يجب الاتصال بمدير النظام لإعادة تعيين 2FA:

```bash
# المدير يشغل:
python scripts/reset_2fa.py --username YOUR_USERNAME
```

---

## 💾 النسخ الاحتياطي المشفر

### نظرة عامة

النظام يستخدم **AES-256-GCM** لتشفير النسخ الاحتياطية:

```
قاعدة البيانات
    ↓
ضغط (gzip) - توفير المساحة
    ↓
تشفير (AES-256-GCM) - الحماية
    ↓
حساب Checksum (SHA-256) - التحقق من السلامة
    ↓
ملف .encrypted
```

### إنشاء نسخة احتياطية مشفرة

#### الطريقة 1: من الواجهة (موصى بها)

1. **ملف → نسخة احتياطية مشفرة…**
2. انتظر حتى تظهر رسالة النجاح
3. سيتم حفظ الملف في: `data/backups/`

**اسم الملف:**
```
backup_YYYYMMDD_HHMMSS.encrypted
مثال: backup_20251118_143052.encrypted
```

#### الطريقة 2: من سطر الأوامر

```bash
python scripts/create_encrypted_backup.py
```

**خيارات متقدمة:**
```bash
# تحديد مسار مخصص
python scripts/create_encrypted_backup.py --output /path/to/backup.encrypted

# مع بيانات وصفية
python scripts/create_encrypted_backup.py --metadata "نسخة قبل الترقية"
```

### استعادة نسخة احتياطية

#### الطريقة 1: من الواجهة

1. **ملف → استعادة نسخة مشفرة…**
2. اختر ملف `.encrypted`
3. أكّد الاستعادة (تحذير: سيتم استبدال البيانات الحالية)
4. انتظر حتى تكتمل العملية
5. البيانات ستُحدّث تلقائياً

**⚠️ تحذير:**
```
سيتم إنشاء نسخة طوارئ من قاعدة البيانات الحالية تلقائياً
قبل الاستعادة للحماية من فقدان البيانات.

الملف: data/backups/emergency_YYYYMMDD_HHMMSS.db
```

#### الطريقة 2: من سطر الأوامر

```bash
python scripts/restore_encrypted_backup.py backup_20251118_143052.encrypted
```

### إدارة مفاتيح التشفير

#### موقع المفتاح الافتراضي

```
config/backup_encryption.key
```

**⚠️ هام جداً:**
- بدون المفتاح، لا يمكن استعادة النسخ الاحتياطية
- يجب حفظ المفتاح في مكان آمن منفصل

#### تصدير المفتاح

```bash
python scripts/export_backup_key.py --output /safe/location/backup.key
```

#### استيراد مفتاح

```bash
python scripts/import_backup_key.py --key /safe/location/backup.key
```

#### تدوير المفتاح (تغيير المفتاح)

```bash
# إنشاء مفتاح جديد وإعادة تشفير النسخ القديمة
python scripts/rotate_backup_key.py
```

### استراتيجية النسخ الاحتياطي 3-2-1

**القاعدة الذهبية:**

```
3 نسخ على الأقل
2 وسائط تخزين مختلفة
1 نسخة خارج الموقع
```

**مثال عملي:**

1. **النسخة 1**: على القرص الصلب المحلي (`data/backups/`)
2. **النسخة 2**: على قرص خارجي (USB/External HDD)
3. **النسخة 3**: في السحابة (Google Drive, OneDrive, Dropbox مع تشفير إضافي)

#### مثال تطبيقي

**يومياً:**
```bash
# نسخ احتياطي محلي
python scripts/create_encrypted_backup.py
```

**أسبوعياً:**
```bash
# نسخ إلى قرص خارجي
python scripts/create_encrypted_backup.py --output E:/Backups/weekly_backup.encrypted
```

**شهرياً:**
```bash
# نسخ إلى السحابة (يُرفع يدوياً)
python scripts/create_encrypted_backup.py --output monthly_backup.encrypted
# ثم ارفعه إلى Google Drive أو OneDrive
```

### جدولة النسخ الاحتياطي التلقائي

#### Windows Task Scheduler

1. افتح **Task Scheduler**
2. **Create Basic Task**
3. الاسم: "Logical Version Auto Backup"
4. التكرار: يومياً في الساعة 2 صباحاً
5. الإجراء: **Start a program**
6. البرنامج:
   ```
   C:\Users\...\الإصدار المنطقي trae\.venv\Scripts\python.exe
   ```
7. المعاملات:
   ```
   scripts/create_encrypted_backup.py
   ```
8. ابدأ في:
   ```
   C:\Users\...\الإصدار المنطقي trae
   ```

#### Linux/macOS (cron)

```bash
# تحرير crontab
crontab -e

# إضافة السطر التالي (يومياً الساعة 2 صباحاً)
0 2 * * * cd /path/to/logical-version && .venv/bin/python scripts/create_encrypted_backup.py
```

### التحقق من سلامة النسخ الاحتياطية

```bash
# اختبار استعادة دون تطبيق التغييرات
python scripts/verify_backup.py backup_20251118_143052.encrypted
```

**النتيجة:**
```
✅ الملف قابل للقراءة
✅ التشفير سليم
✅ التحقق من Checksum نجح
✅ البيانات الوصفية صحيحة
✅ قاعدة البيانات سليمة

النسخة الاحتياطية صالحة للاستخدام ✓
```

### حل المشاكل

#### خطأ: "فشل التحقق من Checksum"

**السبب:** الملف تالف أو تم تعديله

**الحل:**
1. جرب نسخة احتياطية أخرى
2. تحقق من سلامة وسيط التخزين
3. استخدم أدوات استعادة البيانات إذا لزم الأمر

#### خطأ: "مفتاح التشفير غير صحيح"

**السبب:** مفتاح خاطئ أو مفقود

**الحل:**
1. تأكد من استخدام نفس مفتاح التشفير
2. جرب استيراد المفتاح من النسخة الاحتياطية
3. إذا فُقد المفتاح، النسخة غير قابلة للاستعادة

#### الملف كبير جداً

**الحل:**
```bash
# تنظيف قاعدة البيانات أولاً
python scripts/optimize_database.py

# ثم إنشاء النسخة
python scripts/create_encrypted_backup.py
```

---

## 🔒 أفضل الممارسات الأمنية

### كلمات المرور

**✅ افعل:**
- استخدم 12+ حرف على الأقل
- اجمع بين أحرف كبيرة وصغيرة وأرقام ورموز
- استخدم مدير كلمات مرور
- غيّر كلمة المرور كل 90 يوماً
- لا تستخدم كلمات المرور المكررة

**❌ لا تفعل:**
- لا تشارك كلمة مرورك
- لا تحفظها في ملف نصي
- لا تستخدم معلومات شخصية (اسمك، تاريخ ميلادك)
- لا تكتبها على ورقة قرب الجهاز

### 2FA

**✅ افعل:**
- فعّل 2FA لجميع الحسابات
- احفظ رموز الاسترداد فوراً
- استخدم تطبيقات موثوقة (Google Authenticator, Authy)
- احتفظ بنسخة من رموز الاسترداد في مكان آمن

**❌ لا تفعل:**
- لا تستخدم SMS كطريقة 2FA الوحيدة (غير آمنة)
- لا تشارك رموز 2FA مع أحد
- لا تحفظ رموز الاسترداد رقمياً على نفس الجهاز

### النسخ الاحتياطي

**✅ افعل:**
- أنشئ نسخ احتياطية دورية (يومياً على الأقل)
- اختبر الاستعادة شهرياً
- احفظ النسخ في 3 أماكن مختلفة
- شفّر جميع النسخ الخارجية
- احتفظ بمفتاح التشفير منفصلاً

**❌ لا تفعل:**
- لا تحفظ جميع النسخ على نفس القرص
- لا تهمل اختبار الاستعادة
- لا تشارك مفاتيح التشفير عبر البريد
- لا تعتمد على نسخة واحدة فقط

### الصلاحيات

**✅ افعل:**
- امنح أقل الصلاحيات الضرورية
- راجع الصلاحيات دورياً
- أنشئ حسابات منفصلة لكل موظف
- عطّل الحسابات غير النشطة
- سجّل جميع التغييرات الحساسة

**❌ لا تفعل:**
- لا تستخدم حساب المدير للعمل اليومي
- لا تشارك حسابات بين الموظفين
- لا تترك حسابات قديمة نشطة
- لا تمنح صلاحيات مدير دون داعٍ

---

## 📋 قائمة مرجعية للأمان

### شهرياً

- [ ] اختبار استعادة نسخة احتياطية
- [ ] مراجعة السجلات الأمنية
- [ ] فحص الحسابات غير النشطة
- [ ] التحقق من مساحة النسخ الاحتياطي

### ربع سنوياً

- [ ] تغيير كلمات المرور
- [ ] مراجعة الصلاحيات
- [ ] تحديث مفاتيح التشفير (دوران المفاتيح)
- [ ] تدريب المستخدمين على الأمان

### سنوياً

- [ ] مراجعة أمنية شاملة
- [ ] اختبار اختراق (Penetration Testing)
- [ ] تحديث سياسات الأمان
- [ ] تدقيق النسخ الاحتياطية وإزالة القديمة

---

## 🆘 جهات الاتصال للطوارئ

**مشاكل أمنية:**
- البريد: security@logicalversion.com
- الهاتف: +966-XX-XXXX-XXXX (خط الطوارئ)

**الدعم الفني:**
- البريد: support@logicalversion.com
- Discord: https://discord.gg/logicalversion

**في حالة الاختراق:**
1. افصل الجهاز من الشبكة فوراً
2. اتصل بفريق الأمان
3. لا تحذف أي ملفات (قد تكون أدلة)
4. غيّر جميع كلمات المرور من جهاز آخر آمن

---

© 2025 Logical Version Team. جميع الحقوق محفوظة.
